# Anarchos

A Nostr-backed anarchist community app with a web client, local SQLite indexer, relay publishing, and deployment helpers.

## Security note

Before deploying, create a local `.env` from `.env.example`. Do not commit real secrets, local databases, logs, or generated build artifacts.
